import { ProdutoUpgradeStatements } from './produto.upgrade.statements';

describe('ProdutoUpgradeStatements', () => {
  it('should create an instance', () => {
    expect(new ProdutoUpgradeStatements()).toBeTruthy();
  });
});
